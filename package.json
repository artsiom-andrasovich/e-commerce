{
  "name": "e-commerce",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter @app/gateway run dev\" \"pnpm --filter @app/products-service run dev\" \"pnpm --filter @app/users-service run dev\" \"pnpm --filter @app/client run dev\" \"pnpm --filter @app/uploads-service run dev\"",
    "test": "concurrently \"pnpm --filter @app/products-service run test\" \"pnpm --filter @app/client run test\""
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
